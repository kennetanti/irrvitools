#!/bin/bash

LIVE_DIR=`pwd`

cd ~/irrvitools
case "$1" in
status)
	git fetch origin
	git status
	;;
upload)
	git add .
	git commit -m $2
	git push
;;
grab)
	git pull
;;
esac

chmod -R +x bin

cd $LIVE_DIR
